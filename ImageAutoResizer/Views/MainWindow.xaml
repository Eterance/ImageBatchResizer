<Window x:Class="ImageBatchResizer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageBatchResizer.Views"
        xmlns:converters="clr-namespace:ImageBatchResizer.Converters"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="800" MinWidth="600" MinHeight="600">

    <Window.Resources>
        <converters:IsCheck2VisibilityConverter x:Key="IsCheck2VisibilityConverter"/>
        <converters:ProgressBarValueRound x:Key="ProgressBarValueRound"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>


        <GridSplitter Grid.Row="1"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center"
                      Height="1"
                      Visibility="{Binding ElementName=ConsoleCheckBox, Path=IsChecked, Converter={StaticResource IsCheck2VisibilityConverter}}"/>

        <Grid Grid.Row="2" 
              Visibility="{Binding ElementName=ConsoleCheckBox, Path=IsChecked, Converter={StaticResource IsCheck2VisibilityConverter}}">
            <TextBox IsReadOnly="True"></TextBox>
        </Grid>


        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="1" 
                       HorizontalAlignment="Stretch"
                       LastChildFill="False"
                       Margin="8 0 8 0">
                <Button Padding="8 0 8 0"
                        Margin="4" 
                        DockPanel.Dock="Right">
                    开始转换
                </Button>
                <Button Padding="8 0 8 0"
                        Margin="4" 
                        DockPanel.Dock="Right">
                    打开文件夹（覆盖）
                </Button>
                <Button Padding="8 0 8 0"
                        Margin="4" 
                        DockPanel.Dock="Right">
                    打开文件夹（追加）
                </Button>
                <CheckBox x:Name="ConsoleCheckBox"
                          IsChecked="True"
                          DockPanel.Dock="Left"
                          VerticalContentAlignment="Center">
                    <Label>控制台</Label>
                </CheckBox>
                <Button Padding="8 0 8 0"
                        Margin="4" 
                        DockPanel.Dock="Left"
                        IsEnabled="{Binding ElementName=ConsoleCheckBox, Path=IsChecked}">
                    清空控制台
                </Button>
            </DockPanel>
        </Grid>

        <Grid Grid.Row="4" Margin="0 8 0 8" IsEnabled="{Binding IsEnableParametersPanel}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Vertical" Margin="4 0 4 0">
                <Label FontSize="14">输入筛选参数</Label>
                <CheckBox x:Name="InputResolutionUpperLimitCheckBox" 
                          VerticalAlignment="Stretch" 
                          VerticalContentAlignment="Center"
                          Margin="4 4 4 8"
                          IsChecked="{Binding IsEnableInputResUpperLimit}">
                    <TextBlock TextWrapping="WrapWithOverflow">分辨率上限</TextBlock>
                </CheckBox>
                <Grid IsEnabled="{Binding ElementName=InputResolutionUpperLimitCheckBox, Path=IsChecked}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0"
                           HorizontalAlignment="Left">宽</Label>
                    <TextBox Grid.Column="1"
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Center">10000</TextBox>
                    <Label Grid.Column="2"
                           HorizontalAlignment="Left">长</Label>
                    <TextBox Grid.Column="3"
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Center">8000</TextBox>
                </Grid>

                <CheckBox x:Name="InputResolutionLowerLimitCheckBox" 
                          VerticalAlignment="Stretch" 
                          VerticalContentAlignment="Center"
                          Margin="4 4 4 8"
                          IsChecked="{Binding IsEnableInputResLowerLimit}">
                    <TextBlock TextWrapping="WrapWithOverflow">分辨率下限</TextBlock>
                </CheckBox>
                <Grid IsEnabled="{Binding ElementName=InputResolutionLowerLimitCheckBox, Path=IsChecked}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0"
                           HorizontalAlignment="Left">宽</Label>
                    <TextBox Grid.Column="1"
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Center">1920</TextBox>
                    <Label Grid.Column="2"
                           HorizontalAlignment="Left">长</Label>
                    <TextBox Grid.Column="3"
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Center">1080</TextBox>
                </Grid>

                <CheckBox VerticalAlignment="Stretch" 
                          VerticalContentAlignment="Center"
                          Margin="4 8 4 8"
                          IsChecked="{Binding IsResLimitationAdaptPortraitImage}">
                    <TextBlock TextWrapping="WrapWithOverflow">分辨率限制自适应竖图</TextBlock>
                </CheckBox>

                <Label>读取格式</Label>
                <WrapPanel Margin="4">
                    <CheckBox Margin="4" IsChecked="{Binding IsAcceptBmp}">Bmp</CheckBox>
                    <CheckBox Margin="4" IsChecked="{Binding IsAcceptJpeg}">Jpeg</CheckBox>
                    <CheckBox Margin="4" IsChecked="{Binding IsAcceptPng}">Png</CheckBox>
                    <CheckBox Margin="4" IsChecked="{Binding IsAcceptTga}">Tga</CheckBox>
                    <CheckBox Margin="4" IsChecked="{Binding IsAcceptTiff}">Tiff</CheckBox>
                    <CheckBox Margin="4" IsChecked="{Binding IsAcceptWebP}">WebP</CheckBox>
                </WrapPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="8 0 8 0">
                <Label>模式</Label>
                <WrapPanel Orientation="Horizontal">
                    <RadioButton x:Name="FileSizeModeRadioButton"
                                 IsChecked="True"
                                 Margin="4"
                                 VerticalContentAlignment="Center">
                        <Label>文件大小优先</Label>
                    </RadioButton>
                    <RadioButton x:Name="ResModeRadioButton"
                                 Margin="4"
                                 VerticalContentAlignment="Center">
                        <Label>分辨率优先</Label> 
                    </RadioButton>
                </WrapPanel>

                <StackPanel Orientation="Vertical"
                            Visibility="{Binding ElementName=FileSizeModeRadioButton, Path=IsChecked, Converter={StaticResource IsCheck2VisibilityConverter}}">
                    <Label>
                        <TextBlock TextWrapping="WrapWithOverflow">目标大小区间（KiB）</TextBlock>
                    </Label>
                    <Grid Margin="0 4 0 4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 VerticalContentAlignment="Center"
                                 HorizontalContentAlignment="Center">300</TextBox>
                        <Label Grid.Column="1" 
                               HorizontalAlignment="Left">-</Label>
                        <TextBox Grid.Column="2"
                                 VerticalContentAlignment="Center"
                                 HorizontalContentAlignment="Center">350</TextBox>
                    </Grid>

                    <DockPanel Margin="0 8 0 8">
                        <Label DockPanel.Dock="Left">
                            <TextBlock TextWrapping="WrapWithOverflow">二分法尝试次数上限</TextBlock>
                        </Label>
                        <TextBox DockPanel.Dock="Left"
                                 Margin="4 0 0 0"
                                 VerticalContentAlignment="Center"
                                 HorizontalContentAlignment="Center">20</TextBox>
                    </DockPanel>

                    <CheckBox Margin="0 4 0 4">
                        <TextBlock TextWrapping="WrapWithOverflow">删除直接转换后小于目标大小区间的文件</TextBlock>
                    </CheckBox>

                    <CheckBox x:Name="FileSizeModeOutputResolutionLowerLimitCheckBox" Margin="0 4 0 4">分辨率下限</CheckBox>
                    <StackPanel IsEnabled="{Binding ElementName=FileSizeModeOutputResolutionLowerLimitCheckBox, Path=IsChecked}">

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0"
                           HorizontalAlignment="Left">宽</Label>
                            <TextBox Grid.Column="1"
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Center">1920</TextBox>
                            <Label Grid.Column="2"
                           HorizontalAlignment="Left">长</Label>
                            <TextBox Grid.Column="3"
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Center">1080</TextBox>
                        </Grid>

                        <CheckBox VerticalAlignment="Stretch" 
                          VerticalContentAlignment="Center"
                          Margin="0 8 0 4">
                            <Label Grid.Column="1" 
                               HorizontalAlignment="Left">
                                <TextBlock TextWrapping="WrapWithOverflow">分辨率限制自适应竖图</TextBlock>
                            </Label>
                        </CheckBox>

                        <Label>
                            <TextBlock TextWrapping="WrapWithOverflow">输出图片分辨率小于下限时：</TextBlock>
                        </Label>
                        <RadioButton IsChecked="True" 
                                     VerticalContentAlignment="Center">
                            <Label>
                                <TextBlock TextWrapping="WrapWithOverflow">保留此图片，什么也不做</TextBlock>
                            </Label>
                        </RadioButton>
                        <RadioButton VerticalContentAlignment="Center">
                            <Label>
                                <TextBlock TextWrapping="WrapWithOverflow">删除此图片</TextBlock>
                            </Label>
                        </RadioButton>
                        <RadioButton VerticalContentAlignment="Center">
                            <Label>
                                <TextBlock TextWrapping="WrapWithOverflow">忽略目标大小区间限制，重新调整至分辨率下限</TextBlock>
                            </Label>
                        </RadioButton>

                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Vertical" Visibility="{Binding ElementName=ResModeRadioButton, Path=IsChecked, Converter={StaticResource IsCheck2VisibilityConverter}}">
                    <TextBlock TextWrapping="WrapWithOverflow" 
                               Margin="0 4 0 4">目标分辨率</TextBlock>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0"
                           HorizontalAlignment="Left">宽</Label>
                        <TextBox Grid.Column="1"
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Center">1920</TextBox>
                        <Label Grid.Column="2"
                           HorizontalAlignment="Left">长</Label>
                        <TextBox Grid.Column="3"
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Center">1080</TextBox>
                    </Grid>

                    <CheckBox VerticalAlignment="Stretch" 
                          VerticalContentAlignment="Center"
                          Margin="0 8 0 4">
                        <Label HorizontalAlignment="Left">
                            <TextBlock TextWrapping="WrapWithOverflow">分辨率限制自适应竖图</TextBlock>
                        </Label>
                    </CheckBox>

                    <CheckBox x:Name="ResModeOutputResolutionLowerLimitCheckBox"
                          VerticalContentAlignment="Center"
                              Margin="0 4 0 4">
                        <Label HorizontalAlignment="Left">
                            <TextBlock TextWrapping="WrapWithOverflow">目标大小区间（KiB）</TextBlock>
                        </Label>
                    </CheckBox>
                    <StackPanel IsEnabled="{Binding ElementName=ResModeOutputResolutionLowerLimitCheckBox, Path=IsChecked}">
                        <Grid Margin="0 4 0 4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                 VerticalContentAlignment="Center"
                                 HorizontalContentAlignment="Center">300</TextBox>
                            <Label Grid.Column="1" 
                               HorizontalAlignment="Left">-</Label>
                            <TextBox Grid.Column="2"
                                 VerticalContentAlignment="Center"
                                 HorizontalContentAlignment="Center">350</TextBox>
                        </Grid>

                        <Label>
                            <TextBlock TextWrapping="WrapWithOverflow">输出图片文件大小超出目标大小区间时：</TextBlock>
                        </Label>
                        <RadioButton IsChecked="True" 
                                     VerticalContentAlignment="Center">
                            <Label>
                                <TextBlock TextWrapping="WrapWithOverflow">保留此图片，什么也不做</TextBlock>
                            </Label>
                        </RadioButton>
                        <RadioButton VerticalContentAlignment="Center">
                            <Label>
                                <TextBlock TextWrapping="WrapWithOverflow">删除此图片</TextBlock>
                            </Label>
                        </RadioButton>
                        <RadioButton x:Name="ResModeIgnoreRadioButton" 
                                     VerticalContentAlignment="Center">
                            <Label>
                                <TextBlock TextWrapping="WrapWithOverflow">忽略目标分辨率限制，重新调整至目标大小区间内</TextBlock>
                            </Label>
                        </RadioButton>

                        <DockPanel Margin="0 8 0 8" 
                                   IsEnabled="{Binding ElementName=ResModeIgnoreRadioButton, Path=IsChecked}">
                            <Label>
                                <TextBlock DockPanel.Dock="Left"
                                   VerticalAlignment="Center">二分法尝试次数上限</TextBlock>
                            </Label>
                            <TextBox DockPanel.Dock="Left"
                                 Margin="4 0 0 0"
                                 VerticalContentAlignment="Center"
                                 HorizontalContentAlignment="Center">20</TextBox>
                        </DockPanel>

                    </StackPanel>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Vertical" Margin="8 0 8 0">
                <TextBlock Margin="0 4 0 4">图片质量</TextBlock>
                <DockPanel Margin="0 4 0 4">
                    <TextBox Width="40" 
                                 HorizontalContentAlignment="Center"
                                 DockPanel.Dock="Left"
                                 Text="{Binding ElementName=QualitySlider, Path=Value}"/>
                    <Slider x:Name="QualitySlider" 
                                Maximum="100" 
                                Minimum="1" 
                                Margin="8 0 8 0" 
                                Value="90" 
                                SmallChange="1" 
                                LargeChange="10"
                                DockPanel.Dock="Right"
                                IsSnapToTickEnabled="True"
                                TickFrequency="1"/>
                </DockPanel>

                <Label>
                    <TextBlock TextWrapping="WrapWithOverflow">输出格式</TextBlock>
                </Label>
                <ComboBox Margin="0 4 0 4"
                          ItemsSource="{Binding FormatList}"
                          SelectedValue="{Binding SelectedFormatModel}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Label>
                    <TextBlock TextWrapping="WrapWithOverflow">采样算法</TextBlock>
                </Label>
                <ComboBox Margin="0 4 0 4"
                          ItemsSource="{Binding ResamplerList}"
                          SelectedValue="{Binding SelectedResamplerModel}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Label>
                    <TextBlock TextWrapping="WrapWithOverflow">适应分辨率时的缩小策略</TextBlock>
                </Label>
                <ComboBox Margin="0 4 0 4"
                          ItemsSource="{Binding ResizeModeList}"
                          SelectedValue="{Binding SelectedResizeModeModel}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Label>
                    <TextBlock TextWrapping="WrapWithOverflow">输出文件夹</TextBlock>
                </Label>
                <DockPanel Height="25">
                    <Button DockPanel.Dock="Right" Margin="4 0 0 0">选择文件夹</Button>
                    <TextBox VerticalContentAlignment="Center">选择文件夹</TextBox>
                </DockPanel>

                <Label>
                    <TextBlock TextWrapping="WrapWithOverflow">输出文件名</TextBlock>
                </Label>
                <TextBox VerticalContentAlignment="Center" Height="25"></TextBox>
                <WrapPanel>
                    <CheckBox VerticalContentAlignment="Center">
                        <Label>原文件名&lt;name&gt;</Label>
                    </CheckBox>
                    <CheckBox VerticalContentAlignment="Center">
                        <Label>序号&lt;index&gt;</Label>
                    </CheckBox>
                    <CheckBox VerticalContentAlignment="Center">
                        <Label>时间日期&lt;time&gt;</Label>
                    </CheckBox>
                    <CheckBox VerticalContentAlignment="Center">
                        <Label>图片质量&lt;quality&gt;</Label>
                    </CheckBox>
                </WrapPanel>

            </StackPanel>
        </Grid>

        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <ProgressBar Grid.Column="0" Grid.ColumnSpan="2" x:Name="ProcessProgressBar">
            </ProgressBar>
            <TextBlock Text="20/1330"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       Grid.Column="0"
                       Margin="0 0 4 0"/>
            <TextBlock Text="{Binding ElementName=ProcessProgressBar, Path=Value, Converter={StaticResource ProgressBarValueRound}}"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center"
                       Grid.Column="1"
                       Margin="4 0 0 0"/>
        </Grid>
    </Grid>
</Window>
